<?xml version="1.0" encoding="utf-8"?>
<!--
  R4 Smelting Compatibility

  Smelter recipe stripping (SmeltWeapon, SmeltApparel, SmeltOrDestroyThing)
  is now handled in code by R4WorkbenchFilterCache.StripSmelterRecipes(),
  which scans ALL ThingDefs at startup and removes those recipes generically.

  This covers vanilla ElectricSmelter AND any modded smelters (e.g. Fueled
  Smelters) without requiring explicit per-mod XML patches.

  This file is kept as a placeholder in case XML-side overrides are needed
  for specific compatibility cases in future.
-->
<Patch>
  <!-- intentionally empty — see R4WorkbenchFilterCache.StripSmelterRecipes() -->
</Patch>
